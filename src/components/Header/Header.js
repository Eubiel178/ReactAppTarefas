import Styles from "./Header.module.css"

function Header() {
    return (
        <header>
            <h1 className={Styles.title}>ADICIONAR TAREFAS</h1>
        </header>
    )
}

export default Header